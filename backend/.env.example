# Server
# PORT=3000
# RUST_LOG=info
STATIC_DIR=../frontend/dist # for development

# Location
LANGUAGE=fi

# Tomorrow.io
TOMORROW_IO_API_KEY=

# Hue Bridge
HUE_BRIDGE_ADDRESS=
HUE_BRIDGE_USER=
# JSON mapping room names to types: {"inside": ["Kitchen"], "outside": ["Garden"], "inside_cold": ["Garage"]}
HUE_ROOM_TYPES='{}'

# Sensor history
# HCC_DB_PATH=hcc.db
# HCC_HISTORY_RETENTION_DAYS=90
