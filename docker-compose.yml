version: '3'
services:
  hcc:
    image: "ghcr.io/eetu/hcc:latest"
    environment:
      - OPEN_WEATHER_API_KEY
      - HUE_BRIDGE_ADDRESS
      - HUE_BRIDGE_USER
      - HUE_BRIDGE_USER_CLIENT_KEY
      - POSITION_LAT
      - POSITION_LON
      - LANGUAGE
    ports:
      - 3000:3000
